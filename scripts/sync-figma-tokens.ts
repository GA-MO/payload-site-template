/**
 * STUB — pulls Figma variables → updates `@theme` block in globals.css.
 *
 * See .claude/skills/sync-figma-tokens/SKILL.md for the intended pipeline and
 * the manual fallback. The skill is the source of truth for the algorithm
 * until this script is wired up.
 */

console.error(
  'sync-figma-tokens is not implemented yet.\n' +
    'Run `/sync-figma-tokens` in Claude and follow the manual fallback in SKILL.md.',
)
process.exit(1)
